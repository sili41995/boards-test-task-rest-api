const enum Endpoints {
  root = '/',
  register = '/register',
  login = '/login',
  logout = '/logout',
  current = '/current',
  dynamicId = 'id',
  dynamicToken = 'token',
}

export default Endpoints;
